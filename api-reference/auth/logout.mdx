---
title: "Выход (Logout)"
api: "POST http://207.154.237.52:8000/auth/logout"
description: "Выход из аккаунта — завершение текущей сессии"
---

## Ответ

<ResponseField name="detail" type="string">
  Сообщение об успешном выходе
</ResponseField>

<RequestExample>
```bash cURL
curl -X 'POST' \
  'http://207.154.237.52:8000/auth/logout' \
  -H 'accept: application/json' \
  -d ''
```

```javascript JavaScript
const response = await fetch(
  "http://207.154.237.52:8000/auth/logout",
  {
    method: "POST",
    headers: { Accept: "application/json" },
  }
);
const result = await response.json();
```

```python Python
import requests

response = requests.post(
    "http://207.154.237.52:8000/auth/logout"
)
result = response.json()
```
</RequestExample>

<ResponseExample>
```json 200
{
  "detail": "Logged out successfully"
}
```
</ResponseExample>
